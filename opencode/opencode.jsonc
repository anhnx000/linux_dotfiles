{
  "$schema": "https://opencode.ai/config.json",
  "permission": "allow",
  "server": {
    "port": 2351,
    "hostname": "127.0.0.1"
    // UI and API are same-origin behind the login proxy on 2349, so no CORS needed.
  }
}
