# Copy sang ~/.config/host-terminal.env rồi đổi giá trị. KHÔNG commit file .env thật.
#   install -m600 host-terminal.env.example ~/.config/host-terminal.env
# Định dạng: user:password
HOST_TERM_CRED=user:CHANGE_ME
